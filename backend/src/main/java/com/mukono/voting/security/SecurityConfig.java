package com.mukono.voting.security;

// Placeholder removed to avoid confusion with main config in com.mukono.voting.config.SecurityConfig
// If a security-specific config is needed later, define it with a distinct name or remove entirely.
class SecurityConfigurationPlaceholder {

}